<h1><%= @post.title %></h1>
<p><%= @post.body %></p>
<% if current_user == @post.user %>
		<%= link_to "edit", posts_path,  class: "btn btn-primary"%>
		<%= link_to "delete post",post_path,method: :delete,class: "btn btn-primary"%>
<% end %>		
